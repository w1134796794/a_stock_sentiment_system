{
  "name": "a-stock-recap-video",
  "private": true,
  "version": "0.1.0",
  "description": "A股每日收盘复盘 9:16 短视频 —— 用 HyperFrames 把分镜 HTML 渲染成 MP4",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "lint": "hyperframes lint",
    "preview": "hyperframes preview",
    "render": "hyperframes render"
  },
  "devDependencies": {
    "hyperframes": "^0.4.41"
  }
}